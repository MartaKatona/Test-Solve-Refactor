const Chai = require('chai');  // if you dont put a path, npm looks first in modules in node_modules library
let Expect = Chai.expect;  // normal js

const TestSolveRefactor = require('../testsolverefactor');